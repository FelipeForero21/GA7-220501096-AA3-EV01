# =========================
# Configuración por defecto: H2 en memoria
# =========================
spring.datasource.url=jdbc:h2:mem:standalone_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA y Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.defer-datasource-initialization=true

# Consola H2
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Contexto del servidor (opcional)
# server.port=8080

# =========================
# Opción MySQL (descomentar para usar)
# =========================
# spring.datasource.url=jdbc:mysql://localhost:3306/standalone_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Bogota
# spring.datasource.username=root
# spring.datasource.password=tu_password
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
